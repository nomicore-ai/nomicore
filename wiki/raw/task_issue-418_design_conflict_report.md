# SA8 设计后冲突复查 — issue #418：`HubConnectionImpl` 拆分 Edge/SessionHost（ADR 0032 决策 1 / spec #415 T2）

- Dispatch：`sa-d32f5f82-4c3a-4d08-b2ac-a3641082f08b`（mabf-sa8 / conflict-gate / iteration 2）
- 复查对象：`wiki/raw/task_issue-418_design.md`（SA1 设计 **iteration 2 修订版**，dispatch `sa-e45dbd41…`，736 行，基线 HEAD `27e012b`）——重点：**为解 SA3 §7 BLOCKER 引入的新缝面成员 `HubSessionEdgePort.openAdmission(namespaceId): Promise<HubOpenAdmission>`**（异步拉取式 admission 路由）及其配套机制重写（D5 到达点建通道 / D4b 三案路由 / D5.4 两量 drain 判定 / D5.5 原生 revoke）
- 裁决：**`clear`**；`requiresConflictRecheck: true`（见 §9/§10）
- 本报告**原位替换** iteration 1 报告（`sa-7e2a6248…`）：iteration 1 裁决中被本版形态整体超越者（注 C 针对「失败 admission 随参数推送」的论证载体、行 6 的 pending 无界缓冲延期、行 16 的 R7 微差登记、§8-R4/R6/R8 义务）以本版 §3/§8 的现行表述为准；被证伪前提的结论不再堆叠为当前结论。仍存续者（注 A 入站缝形态、§8-R2/R3/R5/R7 同类义务）由本版同名条目承继。

---

## 1. Reviewed subject: design

SA1 iteration 2 设计全文（736 行）逐节对照决策集，核心修订 = 采纳 SA3 §7 建议的**方案 A**：整体删除 iteration 1 的「pending 窗口日志 + 结算同步段回放」机制族（二相 OpenEntry、PendingEvent、强制结算、terminate 标记、投影存储、`revoked` 结局），替换为「首个 OPEN **到达点**投递建通道（= HEAD 时序）+ authorize 真实调用单点在 edge + 结局经新 port 成员 `openAdmission` **异步拉取**过缝」。上游输入：SA2 review（`wiki/raw/task_issue-418_sa2_review.md`，**approve**——针对 iteration 1 形态；F1~F4 已解决、F5 MINOR）、SA3 实现报告（`wiki/raw/task_issue-418_sa3_impl.md`，**reject**：§7 设计级 BLOCKER + §8 A1~A6 登记项 + 方案 A 建议）、SA6 契约（approve）。SA2 对 iteration 2 的设计评审按设计 §13 R9' 排在本门禁之侧/之后——SA8 不等待也不派发；本报告以 SA3 的事实性发现（两测试锚、A1~A6）为证据输入独立复核，其设计优劣判断不属 SA8 裁决面。设计 §15 自请的裁决项（§15.1 (a)/(b)/(c)、§15.2、§15.3 五项、§15.4）本报告逐一裁决（§3 行 4/5/10、§8）。

## 2. Inputs and decision set

| 输入 | 路径 | 状态 |
| --- | --- | --- |
| 任务简报 | `wiki/raw/task_issue-418.md` | Issue #418 body + 5 AC；Comments 空、REST comments `[]`（派工单明示）⇒ 无 owner 追加要求、**无 override 通道** |
| 被审设计 | `wiki/raw/task_issue-418_design.md` | SA1 iteration 2（736 行；自述逐条落实 SA3 §7 方案 A，§14 映射） |
| SA3 实现报告 | `wiki/raw/task_issue-418_sa3_impl.md` | **reject**：D5.1/D5.2 结算段回放 vs AC4 在 2/569 既有测试不可同时满足（ac7-faults:53 / issue171-red:183）；方案 A 建议即本版被审机制；A1~A6 登记项本报告逐项承接（§3 行 4/5/16、§8） |
| SA2 评审 | `wiki/raw/task_issue-418_sa2_review.md` | approve（iteration 1 形态；F1~F4 解决 + F5 + N1~N5）；F5 矛盾载体（`revoked`）已随本版机制删除而消解 |
| SA6 契约 | `wiki/raw/task_issue-418_sa6_contract.md` | verdict approve；基线全绿；C0a~C0d + C1~C6 判据 |
| 权威决策 | `docs/adr/0032-transport-decoupling-edge-session-split.md` | **已接受**（决策 1~5 + 否决备选 + 后果节；状态行「listen 模式行为逐字节不变」） |
| 规范 wire 契约 | `docs/protocols/instance-replication-v1.md` | 已接受；§1/§3/§6.3/§7.1/§13/§14/§15.2/§17/§19/§21/§23 重点复读 |
| 术语决策 | `CONTEXT.md` | 「复制 Edge」（:225-227）、「SessionHost」（:229-231）、「路由键契约」（:233-235）本轮逐条复读 |
| 模块规约 | `packages/ws-replication/AGENTS.md`（:14「Keep admission bounded across handshake, ready, backpressure, and drain windows」等 Boundaries 全节）、`packages/replication-protocol/AGENTS.md`、`docs/AGENTS.md`（「Amend or supersede prior decisions explicitly instead of silently contradicting them」）、根 `AGENTS.md` | 明确收录的决策与验证门 |
| 关联 ADR | 0010（架构边界）、0012（身份/插件所有权）、0013/0022（分块传输）、0023（冻结服务表面） | 全部已接受；无 superseded 决策触及本设计 |
| 代码事实（确认用） | HEAD `hub-connection.ts`（`git show`）、`hub-namespace.ts`（零 diff，worktree 即 HEAD）、`frame-io.ts`/`backpressure.ts`、既有两测试锚 | SA8 逐点亲读复核（§3 Evidence 列）；代码只确认现状事实，不替代决策文本 |

工作区核验：HEAD `27e012b6606e48797842a79e11e3505819c34cc6`、分支 `mabf/issue-418`；worktree 含 SA3 iteration 0 未提交实现（`hub-edge.ts`/`hub-session.ts`/`hub-split.ts` 未跟踪，`backpressure.ts`/`frame-io.ts`/`hub-connection.ts` 修改——均为 §11 ALLOW 面）；**DENY 面本轮核验全部零 diff**（`git diff --stat HEAD` 对 `hub-namespace.ts`、`index.ts`、`testing.ts`、`types.ts`、`defaults.ts`、`validate.ts`、`plugin.ts`、`peer-*.ts`、`packages/replication-protocol/src/**`、`docs/**`、`CONTEXT.md` 为空）；既有 73 测试文件零修改（仅 3 个本票新增测试文件未跟踪）。

## 3. Decision analysis

| # | Decision | Clause | Subject behavior | Classification | Evidence | Required action |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | ADR 0032 决策 1 | 沿 `HubChannelHost` 内缝拆分、两半皆 nomicore、listen = 进程内组合、协议状态机单份（:12-14） | §7 D1/D2/D7 维持且较 iteration 1 更强：二相 OpenEntry/PendingEvent/窗口日志/结算回放/投影存储等「通道存在性中间态机械件」整体删除——连接 FSM 唯一在 `hub-edge.ts`、通道 FSM 唯一在零 diff `hub-namespace.ts`、session host 只做容器 + channelHost 组装 + 分派壳 + shim；C0c 增「edge 无 OpenEntry/PendingEvent/窗口帧复现符号」结构断言 | **implements-existing-decision** | ADR 0032:12-14；设计 §7 D1/D2/D7；SA8 复核：HEAD `onOpenNamespace`（`git show` :880-894）= 无通道即 `new HubNamespaceChannel` + `channels.set` + `channel.startOpen(message)` 同步段——本版 `openNamespace` 逐行原样迁移此形态 | §8-R1（C0a~C0d + 结构断言） |
| 2 | ADR 0032 决策 2（出站） | session 以 sequence=0 占位编码、edge mux 重写 `[8..12]`、wire 逐字节不变（:18） | §7 D3 与 iteration 1 **逐字一致**（本版未触碰出站机制）：`emitOne` 字节化 + 大端盖章、单点分配、message 形态薄包装、D3.3 逐路径次序等价账 + R4' 登记维持 | **implements-existing-decision** | ADR 0032:18；设计 §7 D3/D3.3（自述逐字维持）；iteration 1 行 2 裁决的代码锚（`frame-io.ts`/`backpressure.ts`）未被本版触碰 | §8-R1/R9'（R4' 登记面维持） |
| 3 | ADR 0032 决策 2（入站缝形态） | 入站 sequence 由 edge 校验；缝只过 `Uint8Array` 帧与纯 JSON（:16-18，「header-only」字面） | §7 D4a 维持：edge 单点全解码准入（序列检查先于 payload）+ 已解码消息 + wire 序号过缝——iteration 0/1 **注 A** 目的读法裁决延用，范围限定本票进程内 listen | **no-conflict**（注 A 延用） | ADR 0032:18；CONTEXT.md:230；设计 §7 D4a | §8-R5（worker 形态票前置门禁，义务存续） |
| 4 | **ADR 0032 决策 2（4 控制信号 / 缝无接纳信号）——本版核心新面：`port.openAdmission`** | 「四个控制信号（close/terminateUnauthorized/settled/closed）」+「缝无接纳信号（fire-and-forget）」（:18）；否决备选「缝携带接纳信号（sent/deferred/rejected）」（:36） | 新增 session→edge 异步拉取成员 `openAdmission(namespaceId): Promise<HubOpenAdmission>`。SA8 逐层裁决：**(i) 生命周期信号集不变**——close/terminateUnauthorized/settled/closed 四信号在 D6 全部在位、语义未动；**(ii) 非被否决的「接纳信号」**——:36 否决的是**逐帧出站接纳回执**（sent/deferred/rejected，背压语义，否决理由「跨线程信号回到 ingress 时队列又积一批、相对 fire-and-forget 无收益多契约」）；本成员为每 (连接, ns) **恰一次**的授权结局载体（决策 3 既有义务面），频率与背压路径无关、结构性有界（台账 ≤ 通道数），否决理由不附着；**(iii) 成员类别成立**——port 既有同类「查询/拉取成员」先例（`connectionState()`/`dataGateOpen()`/`chunkedUpdateNegotiated()`/`bufferedAmount()`/`connectionId()`，全部经 iteration 1 行 4 裁决为 `HubChannelHost` 24 成员逐名迁移面），本成员为其中**首个异步**成员：进程内 promise = 函数调用缝的延迟返回值，缝形态未变；**(iv) 数据类合规**——`HubOpenAdmission`（authorized/denied/throw）纯 JSON、无 live 对象过缝；promise 永不 reject（throw 以值承载），唯一 reject 面 = 台账缺失（不变量破坏的 fail-loud，映射 startOpen catch → INTERNAL_ERROR，无静默 fallback）。**结论：成员本身超出 iteration 1「逐名迁移 + 字节化」口径、确属新行为面缝成员（设计如实按 R9 口径自报并自设门禁——处置正确）**；经裁决为**决策 3 authorize 成员的合法跨半边载体**（见行 5），决策 2 的三类约束（生命周期信号集 / fire-and-forget / 纯 JSON 数据类）均不冲突 | **implements-existing-decision**（决策 3 载体义务；见行 5 注 C'——本报告核心裁决） | ADR 0032:18、:36；设计 §7 D1 port 定义、§7 D6 新增行、§15.1；SA8 复核：HEAD channelHost `authorize` 第 7 成员（`git show` :518-519 `authorize: (instanceIdentity, namespaceId) => hub.authorize(...)`）——本成员即该注入面成员跨缝的载体；`connectionState` 等查询成员先例（D1 port 面） | §8-R5（worker 形态下该成员承载实质通信成本，随前置门禁重新裁决）+ §8-R9'（落地核对） |
| 5 | **ADR 0032 决策 3（authorize/OPEN 管线）——本版核心重写面（注 C' 裁决）** | authorize 在 edge 端调用、OPEN 全解码、**「未授权 OPEN 不过缝，edge 复现 NAMESPACE_UNAUTHORIZED wire 行为并持拒绝闩锁」**、session shim 以闭包回放 edge 传入的预授权投影、`HubNamespaceChannel` 零改动、OPEN 准入管线为 edge 职责（:20-22）；否决备选自文「投影仍传入，但语义是 edge 授权结果的传递」（:35） | §7 D5 重写为：edge `beginAdmission`（首 OPEN 到达点同步发起唯一真实 authorize + 建 `{promise, resolve}` 台账）→ **无条件立即** `sink.openNamespace(message)`（session = HEAD `onOpenNamespace` 逐行原样：无通道即同步建 + `startOpen`）→ 通道 `'opening'` 内 shim 在 `startOpen` 首 await 处**同步**调 `port.openAdmission(nsId)` 拉取结算结局（authorized → 完整投影 / denied → `{ok:false}` / throw → reject）。**注 C'（iteration 1 注 C 的延伸裁决）**：本版形态下 OPEN 在 authorize 结局产出**之前**过缝、投影以**拉取**交付——机制句字面读法在本版不可采，理由五点：(1) **字面读法使已接受决策集自相矛盾，且本版更直接**——iteration 1 已证「未授权 OPEN 不过缝 ⇒ 通道结构性不存在」迫使「edge 逐 kind 复现 `'opening'` 行矩阵（决策 1 明文否决的 fork）」或「缓冲后丢弃（违背状态行逐字节不变 + AC4）」二选一；本版新增第三重矛盾：**通道在场性与到达点效应本身已是既有测试冻结面**——SA8 本轮亲读复核 `ws-replication-ac7-faults.test.ts`（授权门闩悬挂时注入 UPDATE 后 `hubFrames('ERROR')` 必含 `NAMESPACE_STATE_VIOLATION`，:53）与 `ws-replication-issue171-red.test.ts`（`untilMicrotask(() => channelState() === 'opening')` 前置锚 :183、迟归续体 lease 释放锚 :194）——字面读法直接与 AC4「不改而绿」冲突，无待推导；(2) **决策 3 全部目的经代码核验证实保全且更强**：(i) 真实 authorize 单点在 edge、每 (连接, ns) 恰一次、入参逐值一致（edge 直调 `authorize(authenticatedInstanceId, nsId)` ≡ HEAD `host.authorize(host.peerInstanceId(), nsId)`，`peerInstanceId: () => this.authenticatedInstanceId` :518 已核）；session 结构性不可达真实授权器——shim 只能拉取已结算结局，无法注入/影响/重试授权（结构性论证成立）；(ii) 被拒 ns **零会话资源唤起**——shim `{ok:false}` → 零 diff `startOpen` 自身 `!authz.ok ∨ !permissions.read` 判别在 `registry.open` **之前**短路（SA8 亲读 `hub-namespace.ts:355-359` 判别 → `:361` 起 registry.open，注释明示 D-H1；「会话资源」= Registry lease/ReplicationSession/runtime 订阅——被拒 ns 仅存在一个终态通道对象，与 HEAD 同款：HEAD 也在到达点同步建通道后于 `'opening'` 内 await authorize）；(iii) `NAMESPACE_UNAUTHORIZED` wire 行为与闩锁由单份零 diff 通道原生产出（`finishOpenError` :483-503 + `onOpen` 终态/closing 矩阵 + `isQuietState` 静默门）；(iv) 准入结算管线（authorize 发起、结局映射 `{ok ∧ read → authorized, 否则 denied, throw → throw}`、台账）全在 edge；(v) `hub-namespace.ts` 零 diff（DENY + 本轮 worktree 核验零 diff）；(3) **决策文本自含依据延用**——:35「投影仍传入，但**语义是 edge 授权结果的传递**」：拉取交付传递的仍是 edge 授权结果，语义本体（授权结果的传递）不变，authorized 分支即「预授权投影」逐字保持（`HubOpenAdmission.authorization` 携完整 ok-投影）；(4) **词法类内**——结局纯 JSON（同 iteration 1 注 C 第 4 点），落在决策 2「Uint8Array 帧与纯 JSON」允许类内；(5) **范围限定**——进程内 listen 组合（issue body「缝 = 函数调用」）：被拒 ns 代价 = 一次函数调用 + 一个终态通道对象（HEAD 同款）；worker 形态下「未授权 OPEN 不应唤醒 session worker」承载实质目的，**必须重新过 SA8 前置门禁**。**同步次序不变量经代码核验成立**：`startOpen` 为同步方法内 `void (async () => …)` IIFE（:335-343），同步前缀执行至首 `await this.host.authorize(...)`——shim 的 `port.openAdmission` 调用发生在 `sink.openNamespace` 同一同步段内，故设计 D5.1「① beginAdmission 先于 ② 投递」的次序约束是必要且充分的（违反时台账缺失 → 响亮 reject → INTERNAL_ERROR，fail-loud 不静默）；再 OPEN 合流走 HEAD 既有 openWaiters（`onOpen` `'opening'` 分支 :309-313 已核——不重复 authorize/registry.open，并发 OPEN 上界乘既有面承载） | **implements-existing-decision**（注 C' 目的读法 + 五点依据；机制句表述差 → §8-R7'/R8'） | ADR 0032:20-22、:35；设计 §7 D5.1-D5.3/D5.7、§15.1；SA8 亲读：`hub-namespace.ts:335-375`（startOpen IIFE 结构/throw→INTERNAL_ERROR/`!ok ∨ !read` 短路/D-H1 注释）、`:289-332`（re-OPEN 矩阵）、`:529-531`（isOpenAborted）；HEAD `hub-connection.ts:880-894`/`:518-519`；两测试锚（ac7-faults:32-56、issue171-red:177-210） | §8-R1（C0a/C0b/C0d 新缝 stub 形态 + C2c/C2d + M1 到达点断言）+ §8-R5 + §8-R7' |
| 6 | ADR 0032 决策 3（pending 有界缓冲、并发 OPEN 上界）+ `ws-replication/AGENTS.md`:14「Keep admission bounded across handshake, ready, backpressure, and drain windows」 | OPEN 准入管线含 pending 有界缓冲与并发 OPEN 上界，全部为 edge 规范职责（:22）；模块规约要求四窗口准入有界 | **iteration 1 的延期（注 B/§8-R4）本版就地了结**：pending 全帧窗口日志不存在（窗口帧到达点即投递、零缓冲——ready 窗口）；唯一新增 admission 面 = per-ns 一次性 `{promise, resolve}` 台账（≤ 通道数、与 `channels` Map 同阶——`channels.set` 唯一写点即 `onOpenNamespace`，HEAD 既有只增不减增长面，台账与之锁步）；再 OPEN 合流 = HEAD 既有 openWaiters（OPEN-only 先存面，:309-313 已核）；handshake（早到帧 ≤16）/backpressure（额度账本）/drain（drain 门）三窗口全部既有面零变化。SA8 核验：台账每记录 = 一个至多结算一次的 promise + 结算闭包，连接生命周期内随连接对象整体 GC——无新无界面、无跨窗口积压面。设计同时承诺收尾措辞为「决策 3 pending 有界缓冲条款在本票无新增适用面」而非「全部兑现」——如实 | **implements-existing-decision**（有界准入纪律由构造满足；iteration 1 延期义务就地关闭） | ADR 0032:22；`packages/ws-replication/AGENTS.md`:14（本轮亲读）；设计 §7 D5/§13 R2；`hub-connection.ts`（早到帧 admission 既有）；`onOpen` 合流分支已核 | §8-R4'（worker 形态票义务重入条件登记）+ §8-R1（C0d 锁步断言） |
| 7 | ADR 0032 决策 4（路由键契约） | 定偏移只读提取、OPEN 全解码、ERROR 特例、路由点逐字节复现单体两分支、codec 守卫测试（:24-26） | §7 D4b 路由由四案减为**三案**（R-pending 随窗口日志删除——pending 期通道在场，帧按 R-delivered 即时投递）：R-delivered（台账命中 ⟺ 首 OPEN 已投递 ⟺ 通道在场，**authorize 在途亦然**——窗口帧即时进入零 diff 通道 FSM，§2.2 矩阵到达点承载）；R-none（edge 逐字节复现合成 `NAMESPACE_STATE_VIOLATION` + 事件——HEAD `withChannel` :896-919 逐符号迁移，SA8 iteration 1 已核）；R-violation（D4a 单点全解码承接）。偏移表 `[21,56)`/`[22,57)`/`0x23` 维持；守卫测试入 ALLOW。**路由事实源 = edge 本地 admission 台账**（不读 `sink.channels` 投影）——路由为 edge 规范职责、worker 形态结构前提；台账 ⟺ channels 锁步不变量由 D5.1 同步段 + C0d 断言钉死 | **implements-existing-decision** | ADR 0032:24-26；协议 §4；SA6 C4a/C4b；设计 §7 D4a/D4b；HEAD `withChannel`/ERROR 特例（iteration 1 已核锚） | §8-R1（守卫测试 + C4 锚 + C0d）+ §8-R9'（实现核对：edge 路由/drain 判定读台账、不读 channels 投影） |
| 8 | CONTEXT.md「复制 Edge」术语（:225-227） | 职责清单（OPEN 准入：全解码 + authorize + sink 路由）+ `_Avoid_`（薄 socket adapter/宿主自实现/解码 OPEN/ERROR 以外 payload） | edge 持准入台账 + 唯一真实 authorize + 路由（D5.1/D5.2/D4b）——「OPEN 准入（全解码 + authorize + sink 路由）」逐点吻合；payload 语义重 CPU 全留 session 零 diff 通道；连接级帧不上缝；不持 Registry | **no-conflict** | CONTEXT.md:225-227；设计 §7 D1/D2/D5 | 同 §8-R5 |
| 9 | CONTEXT.md「SessionHost」术语（:229-231） | 「authorize 不在此调用，消费 edge 传入的**预授权投影**」；`_Avoid_`：重检入站 sequence / 连接级帧入 session / 感知 drain / worker 类型 | session 不触达真实授权器（shim 仅拉取已结算结局——结构性）；「消费 edge 传入的预授权投影」在本版为**拉取式消费**（`port.openAdmission` 取回）且时序为 OPEN 先过缝、投影后到——正向描述的表述差较 iteration 1 **扩大**（推送 → 拉取 + 结局前过缝），登记为 §8-R7'/R8' 文本调和义务。`_Avoid_` 四项零触碰（SA8 核验：session 收已校验消息 + wire 序号仅记账不重检；连接级帧不上缝；drain 门在 edge 且先于路由、D5.4 判定在 edge——session 不感知 drain 窗口；零 worker_threads/MessagePort 类型） | **no-conflict**（注 C' 范围内；表述差 → 义务登记） | CONTEXT.md:229-231；设计 §7 D2/D5.3/D6、§13 R8'；SA8 核验 D2/D6/DENY | §8-R7'（词条与机制句一并调和） |
| 10 | ADR 0032 决策 5（观测纪律）（:28-30） | 发射点 = 拥有事实的一侧（连接域与出站 sequence 事件在 edge，namespace 域事件在 session）；字段 append-only；缺面 dormant | iteration 1 行 10 裁决的**进一步强化**：到达点机制下 namespace 域事件全部由零 diff 通道在**到达点/续体点原生次序**产出——iteration 1 的「结算段时序面」不存在，无解释性放面需求；隔离语义单点（`dispatchReplicationObserver`）留 edge 侧执行；`inboundAssemblySlots` 留 edge（listen 不降级）；零新事件型/字段 | **no-conflict** | ADR 0032:28-30；协议 §23.1/§23.4；设计 §7 D2/D8 | §8-R1（observer 恰一性锚） |
| 11 | 协议 §1 不变量 + §3 固定 envelope | 一 message 一帧、单向严格 sequence、每连接每 ns 单生命周期、HELLO 门、20 字节头纪律 | 逐符号迁移保持（行 2/3 载体）；入站 expectedSeq 收口在 edge 解码点；出站单点盖章；HELLO 门/handshaking 分派迁 edge | **no-conflict** | 协议 §1:22-33、§3:47-63；SA6 C1/C2 | 无 |
| 12 | 协议 §6.3 + §21（GOAWAY drain 全通道终态提前完成） | drain 窗口收口纪律；全部 channel 终态提前完成否则 deadline 1001 | §7 D5.4 判定式收敛为两量 `finishDrain() ⟺ ∀ nsId ∈ admissions.keys(): nsId ∈ settledNames`（仅在 settled 信号上执行，drainActive/closedFlag 守卫原样）。SA8 复核同构成立：**pending 案包含式收编**——admission 在途 ⟺ 通道 `'opening'`（authorize await 在通道内）⟺ 非终态 ⟺ 未 settled（I12）→ 迭代式天然阻塞（iteration 1 需独立 pendingNames 的原因是彼时 pending ns 未投递；本版已投递）；已投递 ∧ 未 settled ⟺ HEAD 非终态（opening/bootstrapping/reconciling/live/needs-resync/closing——SA8 亲读 HEAD `maybeFinishDrainEarly` `:640-647`：任一通道 ∉ {closed, conflicted, failed} 即 return，`'opening'`/`'closing'` 均阻塞 ✓）；已投递 ∧ settled ⟺ 终态（两侧放行）；空（无投递）⟺ 空通道（两侧均等 deadline，HEAD 唯一触发点 :529 同构）；台账 keys ≡ channels（锁步）+ settledNames ⊆ 台账 keys 单调——全集同构。drain 门先于路由（被门丢弃帧到不了通道 = HEAD 到不了 withChannel 同构）；deadline fire 不检查通道状态（两侧同） | **no-conflict** | 协议 §6.3:159、§21:683；HEAD `hub-connection.ts:640-647`（本轮 `git show` 亲读）；设计 §7 D5.4 | §8-R3（M2 必做） |
| 13 | 协议 §7.1（OPEN 语义） | 「Hub 必须先 authorization，再从 authorization 结果取得 local owner 并调用 Registry open」；重复 OPEN 合流每请求一答复；未授权不泄露存在性 | 授权先于 registry.open（edge 真实 authorize 到达点发起 → shim 拉取结局 → 仅 authorized 分支达 registry.open——SA8 亲读 `hub-namespace.ts:344-361` 次序 ✓）；waiter 合流/每请求一答复由零 diff 通道原生（`onOpen` 矩阵 + `flushOpenWaitersOk`/`finishOpenError` 逐 waiter）；拒绝仅 `NAMESPACE_UNAUTHORIZED`（不泄露存在性 ✓） | **no-conflict** | 协议 §7.1:163-176；`hub-namespace.ts:289-332, 335-375`（本轮亲读）；设计 §7 D5.3 | §8-R1（C2c/C2d + issue168/172 锚） |
| 14 | 协议 §13/§14（错误与 close 分类注册表） | 注册表值 + WS close 码映射冻结 | 全分类原样迁移；shim throw → `INTERNAL_ERROR`（既有注册表值，HEAD `startOpen` catch 同款 :345-348 已核）；台账缺失 reject → `INTERNAL_ERROR`（同值）；R4' 角落两侧均既有分类（无新码）——缝上无新错误形态 | **no-conflict** | 协议 §13:379-454、§14:456-465；设计 §9 | 无 |
| 15 | 协议 §17（背压/公平调度）+ D3.3 次序账 | 控制保留额度、水位、单帧守卫、统一账本、round-robin；配置键冻结 | D3/D3.3 与 iteration 1 逐字维持（本版未触碰）；R4' 双不可达角落（非暂停控制 ∧ 耗尽 ∧ 编码必败）维持登记；为消差引入行为预检 port 成员的备选被设计自行否决（否则新缝面须再过 SA8） | **no-conflict**（登记差维持） | 协议 §17:564-616；设计 §7 D3.3/§13 R4' | §8-R9'（登记面接受性维持确认） |
| 16 | 协议 §19（Authorization/revoke） | 授权只在 OPEN 时检查；revoke → namespace 终止 ERROR + cleanup | **iteration 1 的 R7 微差登记本版消除**：revoked 强制结算机制整体删除，revoke 走 HEAD 原生路径——窗口期在场通道 `terminateUnauthorized()` 响亮（SA8 亲读 `:1186-1191`：quiet → no-op resolve；否则 `sendNsError('NAMESPACE_UNAUTHORIZED')` + `finalize('failed','protocol-violation')` + `terminationSettled` ✓）；迟归 authorize ok 续体经 D-H1 transient `registry.open` + 显式 lease 回收（`startOpen` :352-372 注释与代码本轮亲读 ✓——authorize 恢复点不拦截 registry.open、中止判别自 registry.open 恢复点起生效、`finishOpenSilently(opened.lease)` 零泄漏）；M1-revoke 臂资源断言重定基为 HEAD 基线（registry spy）——该测试文件为本票 ALLOW 内自有产物（非 DENY 既有面），wire/事件断言不变、资源断言按 SA2 特征化方法论对齐 HEAD 基线，**非断言弱化** | **no-conflict**（微差消除——较 iteration 1 更强） | 协议 §19:637-652；`hub-namespace.ts:352-375, 1186-1191`（本轮亲读）；设计 §7 D5.5、§13 R7 | §8-R1/R9'（M1-revoke 臂对 HEAD 基线核对） |
| 17 | 协议 §15.2 + §21 + issue #229（停机/收口） | Hub close 直接 1001、排空 apply、release lease、不 await 于槽内 | 五路收口同构迁移维持；L1 两处无事件直赋逐点保留、onLivenessLost 不调 clearDrainHandles、`drainDeadline` 死字段不迁移（iteration 1 已核锚，D8 逐字维持）；`sink.close()` 同步前缀 + 异步尾；**窗口期收口由在场通道承载**（`'opening'` 通道随 `onConnectionClosed` 静默收口 = HEAD）+ 迟归 admission 无条件结算传播（D5.6/H1 lease 回收依赖——`issue171-red:194` 锚） | **no-conflict** | 协议 §21:670-685；HEAD `hub-connection.ts:572-1118`（iteration 1 已核）；设计 §7 D2/D5.6/D8 | §8-R3（M3）+ §8-R9'（H1 路径核对） |
| 18 | 协议 §23（observer seam） | 36 型 + 键集冻结、append-only、隔离/时钟纪律 | 零新事件型/字段；到达点原生次序（行 10）；M3 直赋零事件断言维持 | **no-conflict** | 协议 §23.1-§23.7；设计 §7 D8 | 无 |
| 19 | `packages/ws-replication/AGENTS.md`（模块规约） | 生产 API 只经 `src/index.ts`；注入 seam；FSM 不变量；testing 面纪律；:14 准入有界（并入行 6） | 两工厂 + 缝类型仅模块级导出（`HubNamespaceChannel` 不进 index 既有先例）；不进 `src/index.ts`/`src/testing.ts`；依赖注入保持 | **no-conflict** | AGENTS.md（Boundaries/Verification 本轮亲读）；设计 §7 D1/§11 | §8-R2（C5a 冻结门） |
| 20 | `packages/replication-protocol/AGENTS.md` + codec 面 | codec 与传输解耦；公共 API 只经其 `src/index.ts`；wire 值 append-only | `packages/replication-protocol/src/**` 全量 DENY；守卫测试仅进 `test/` | **no-conflict** | AGENTS.md；设计 §11 | 无 |
| 21 | ADR 0010 / 0012 / 0013 / 0022 / 0023 | 架构边界 / 身份与插件所有权 / 分块传输 / 冻结服务表面 | 全部维持 iteration 1 行 21-24 裁决（本版未触碰其载体）：edge 不持 Registry（`HubReplicationEdgeConfig` 无 registry 成员 ✓）；`peerInstanceId = identity`（门 5 后注入）；UPDATE_CHUNK 组装/协商/assembly 槽归属不变；`plugin.ts` 零 diff | **no-conflict** | 各 ADR；设计 §7 D1/D2/D7、§11 | 无 |
| 22 | Issue #418 硬约束（零新公共 API / 零配置 / wire 逐字节 / AC1-AC5） | 任务冻结面 + 等价判据登记面 | §4/§11/§12 完备；**等价判据本版强化**（authorize 窗口效应回到到达点——iteration 1 登记的「跨 ns 微任务交错」「窗口效应（含连接级）推迟至结算」两类差消除）；登记面收敛为两处：R4'（双不可达角落，维持）+ **R6'（新登记：authorize 结算续体微任务跳数 HEAD 1 跳 → 约 3 跳——realAuthorize resolve → edge `.then` 结算 → shim `.then` 映射 → await 恢复）**。SA8 裁 R6' 在等价判据内：仅影响测试泵预算（`settle`/`settleUntil` 显式泵，SA2 N3 实测充裕），不影响 wire 帧序/事件序（帧序在到达点已固定；事件序随通道 FSM 原生次序）——但**两锚的绿必须经实现复核证实**（issue171-red `untilMicrotask` 泵预算容纳 3 跳）；SA6 契约测试文件经本轮 grep 核验**全文无 seam/openNamespace/openAdmission 引用**（缝形态无关——AC4 对该文件成立） | **no-conflict**（登记差均在裁决过的等价类或纯泵预算面内） | `wiki/raw/task_issue-418.md`；SA6 C5a/C5b；设计 §1/§11/§12/§13 R6'；本轮 grep 核验；两测试锚亲读 | §8-R1/R9'（两锚 + 泵预算实现期证实） |

**注 C' 的性质与边界（承接 iteration 1 注 C 的同款限定）**：这不是 override——SA8 未援引也未创建任何外部权威；这是对同一已接受 ADR 内部条款位阶的裁决（状态行「listen 模式行为逐字节不变」+ 决策 1「单份 FSM」+ AC4 冻结的既有测试面，高于决策 3 机制句在进程内形态下的字面），且范围限定于机制句无独立规范效力的进程内 listen 组合。决策 3 机制句（「未授权 OPEN 不过缝，edge 复现…」）与 CONTEXT.md:230 词条在新形态下的表述差（推送 → 拉取、结局后 → 结局前过缝）作为义务登记（§8-R7'），不在本票强制修订（见 §6）。

## 4. Overrides

| Old decision | Override authority | Scope | New obligation |
| --- | --- | --- | --- |
| —（无） | — | — | — |

设计未主张任何 override；Owner 评论集为空（REST `[]` + 简报 §Comments 空），无 Owner 覆盖通道被援引；无新 ADR、无协议版本升级、无决策自含演进条款被触发。§3 注 C' 是**解释性裁决**（同 ADR 内部位阶 + 范围限定），不是 override——SA8 不替 Owner 或 SA1 创建 override。

## 5. Frozen surfaces

| Surface | Must remain unchanged | Evidence | Actual result |
| --- | --- | --- | --- |
| wire 帧字节 | 非 Yjs 载荷帧全帧 hex 金标；envelope 纪律（magic/version/flags/`[8..12]`/payloadLength/reserved） | 协议 §3；SA6 C3a-C3c/C5c/C6a | D3 字节等价机制维持（本版未触碰出站）；DENY `replication-protocol/src/**`；**authorize 窗口效应回到到达点**（较 iteration 1 更贴近 HEAD）——符合 |
| 消息码/capability/错误码/close 码注册表 | `0x01`-`0x42`、`CAP_CHUNKED_UPDATE`、§13.1 17 码、§13.2 全量、§14 映射 | 协议 §5/§6.1/§13/§14 | 零新增/零改号；shim throw 与台账缺失 reject 均映射既有 `INTERNAL_ERROR`；R4' 角落两侧均既有分类——符合 |
| observer 事件面 | 36 型事件 + 键集 + safe-field 清单 + 隔离/时钟纪律 | 协议 §23.1-§23.7 | 零新事件型/字段；到达点原生次序——符合 |
| 公共 API/配置面 | `index.ts` 11 运行时导出、`/testing` 5 导出、`DEFAULT_*` 三常量、`types.ts`/`validate.ts` | SA6 C5a/C5b；ws-replication AGENTS | DENY 钉死（本轮 worktree 核验零 diff）；两工厂 + `hub-split.ts` 缝类型仅模块级导出——符合 |
| 服务表面构造 | `nomicoreHubReplication`/`nomicorePeerReplication` getter 化构造 + freeze | ADR 0023 | `plugin.ts` 零 diff——符合 |
| 通道实现 | `hub-namespace.ts`（`HubNamespaceChannel` + `HubChannelHost` 24 成员注入面） | Issue AC3；ADR 0032 决策 3 | DENY 零 diff（本轮 worktree 核验）；本版全部失败面/闩锁/静默门/drain settled/revoke/D-H1 续体以其为唯一实现——符合（依赖进一步加深，实现期 git diff 门禁为硬门） |
| 既有测试面 | 73 个既有测试文件不改而绿（含 I13 两锚 ac7-faults:53 / issue171-red:183/:194） | AC4；DENY LIST | 本版机制下两锚由构造恢复 HEAD 时序（到达点建通道 + 到达点效应）；SA6 契约文件缝形态无关（本轮 grep 核验）——设计判定成立，**实现复核证实**（§8-R1/R9'） |
| peer 侧 | `peer-connection.ts`/`peer-namespace.ts`（message 形态队列/调度） | ADR 0032 后果节 | DENY + message 形态成员原签名保留——符合 |
| 决策/规范文本 | `docs/adr/**`、`docs/protocols/**`、`CONTEXT.md`、`docs/**` | docs/AGENTS.md | DENY——本票零决策文本变更（注 C' 表述差以 §8-R7' 义务处理；docs/AGENTS.md「amend explicitly」纪律要求其在机制句重新 load-bearing 前落地）——符合 |
| 术语 | 「复制 Edge」「SessionHost」「路由键契约」词条义 | CONTEXT.md:225-235 | `_Avoid_` 零触碰（行 8/9 核验）；正向描述的表述差为登记义务（R7'/R8'）——符合（注 C' 范围内） |

## 6. Evolution requirements

**无 `evolution-required` 项。** 论证：(1) 本设计零 wire/schema/持久化/公共 API/协议文本变更——`openAdmission` 是 `hub-split.ts` **新建内部模块**的缝类型成员（模块级导出，非任何既有公共/规范契约面），其修订不属「改变已有契约」；(2) ADR 0032 决策 3 自身即强制「authorize 在 edge + session shim 回放投影」的跨半边授权传递——载体机制（推送参数/投影存储 vs 异步拉取）ADR 未作规定，:35 自文「投影仍传入，但**语义是 edge 授权结果的传递**」把缝上投影的语义锚定为「授权结果的传递」，拉取交付传递同一结果（authorized 分支词汇逐字保持）；(3) 机制句字面形态经 §3 注 C' 裁决在本票范围（进程内 listen）内与 ADR 自身更高规范面（状态行 + 决策 1）**及 AC4 冻结的既有测试面**（I13 两锚）不相容、即在该范围内不具可执行的规范力，故不构成「需要本变更集修订」的契约改变；(4) 决策 2 的三类缝约束（四信号/fire-and-forget/纯 JSON）经 §3 行 4 逐层核验未被触碰。**文本调和义务不消灭且差扩大**：ADR 0032:22 机制句与 CONTEXT.md:230 词条在实现落地后将描述被超越的形态（推送 → 拉取、结局后 → 结局前过缝），docs/AGENTS.md 纪律要求其在**机制句重新 load-bearing 之前**（即 worker 形态票 SA8 前置门禁之前或之中——彼时「未授权 OPEN 不应唤醒 session worker」承载实质目的）正式落 ADR 修订或澄清附录（含：修订文件、新旧语义〔到达点建通道 + 异步 edge-owned admission 拉取 + 零资源唤起 + 单份 FSM 产失败面〕、兼容性〔wire 逐字节、授权恰一次、资源面〕、失败语义〔denied/throw 映射〕、验证锚〔M1 到达点断言/C2c/C2d/资源零调用〕、保持不变的冻结面）；Host 可在本票实现同变更集自愿先行落 documentation-only 澄清附录（**推荐**——表述差本版扩大，且 worker 形态票的裁决以本版缝形态为前提；非阻断）。pending 有界缓冲义务本版就地了结（§3 行 6），非演进。

## 7. Hard conflicts

**无。** 未发现与 ADR 全集、CONTEXT.md、协议或模块规约不兼容且无合法解决路径的条款。设计 §15 自请项裁决汇总：(a) `openAdmission` 作为决策 3 authorize 跨半边载体——**可接受**（§3 行 4/5：决策 2 三类约束逐层核验不冲突；非被否决的逐帧接纳回执；进程内范围限定）；(b) OPEN 过缝时序前移（结局产出前）对「未授权 OPEN 不过缝」的目的读法延伸——**确认成立且较 iteration 1 更直接**（I13 两锚本身即既有测试冻结面）；(c) 与 R7'/R8' 文本调和义务的联动——**义务存续、差扩大、deadline 不变**（worker 形态票 SA8 前置门禁之前或之中）。§15.2（决策 5 归属）维持 no-conflict 且更强；§15.3 五项：R4 就地了结**确认**（零新增缓冲面 + worker 重入条件登记）、R6/R8 消解**确认**（到达点机制下推迟类不存在；never-settling 授权器与 HEAD 逐点一致——HEAD 自身无 authorize 超时，`'opening'` 通道无限停放为既有形态）、R9 中 R7 复查项撤销**确认**（revoked 机制删除、revoke 走 HEAD 原生 D-H1 路径，iteration 1 行 16 的微差登记随之失效）、R4' 接受性**维持**（双不可达角落登记面不变）、R6' 新登记**接受**（纯泵预算面，无 wire/事件序差——实现复核证实）；§15.4 维持项**确认**（注 A 范围限定与 R5 义务不变；`hub-split.ts` 内部契约面随实现 diff 复审）。SA3 §7 的三条不可行路（edge 合成 = 决策 1 fork / 通道前移 = 当时的越权面 / 改测试 = DENY）本版均未采——设计以正规定型（设计变更）解决，处置正确。

## 8. Required actions

| # | 行动 | 责任落点 |
| --- | --- | --- |
| R1 | **实现期结构证据**：C0a（stub sink 收 `openNamespace(message)` 无 admission 参 + 到达点断言——authorize 桩 resolve 前 stub 已收到 OPEN + `port.openAdmission` 拉取 `{outcome:'authorized'}`）；C0b（stub port `openAdmission` 四形态臂：ok/denied/throw/**reject**——reject 臂响亮 INTERNAL_ERROR 非静默 + denied/throw 臂 `registry.open` 零调用）；C0c（结构断言：edge 无 `OpenEntry`/`PendingEvent`/窗口日志/结算回放符号）；C0d（锁步断言升级：`openNamespace` 返回后 `channels.has(nsId)` 立即为真（authorize 未 resolve 亦然）+ 台账命中 ⟺ 通道在场 + `openNamespace` 必先于该 ns 任何 `namespaceFrame`）；C1~C6 两态全绿；**I13 两锚不改而绿**（ac7-faults:53 / issue171-red:183+:194——后者含 3 跳微任务泵预算证实，R6'）；M1 十臂 × ok/deny + **到达点断言**（响亮臂 wire 帧在 `resolve()` 之前已在 `transport.frames()`）+ M1-revoke 臂对 **HEAD 基线**（stash 实现先采集：transient `registry.open` 恰一次 + lease 释放）+ M2/M3 | 实现票 |
| R2 | **冻结门逐项核对**：`git diff --stat` 对 `hub-namespace.ts`、`index.ts`、`testing.ts`、`types.ts`、`defaults.ts`、`validate.ts`、`plugin.ts`、`peer-*.ts`、session 机械件、`packages/replication-protocol/src/**`、`docs/**`、`CONTEXT.md`、既有 73 测试文件全部为空；两工厂与缝类型不得进 `index.ts`/`testing.ts`（SA6 U4 冲突信号） | 实现票 + 实现后 SA8 复查 |
| R3 | M1/M2/M3 必做维持（F1/F2/F3 验收）：HEAD 基线先行采集方法论维持；M1-revoke 臂资源断言按 HEAD 基线重定基（非弱化——wire/事件断言不变） | 实现票（SA7 动态面） |
| R4' | **有界缓冲义务关闭确认 + 重入条件**：本票新增面 = per-ns 一次性 admission 台账（≤ 通道数，零缓冲）；worker 形态票（或任何引入跨线程 pending/慢授权现实暴露的票）义务**重新进入**并走正式裁决；本票收尾措辞 = 「决策 3 pending 有界缓冲条款在本票无新增适用面」（不得宣称全部兑现——设计 §13 R2 自我承诺，SA8 采纳为收尾措辞门） | 后续票 + Host 追踪 |
| R5 | **worker 形态票前置裁决项（存续且扩面）**：入站「已解码消息 + 序号」缝形态（注 A）+ **`openAdmission` 拉取形态与 OPEN 结局前过缝**（注 C' 第 5 点——彼时该成员承载实质跨线程通信成本、被拒 ns 不应唤醒 session worker 的目的 load-bearing）+ `channels`/`dataFacetOf` 组合成员重塑（决策 5 降级纪律）一并重新过 SA8 | worker 形态票的 SA8 前置门禁 |
| R7' | **文本调和义务（存续且差扩大）**：ADR 0032:22 决策 3 机制句（「未授权 OPEN 不过缝，edge 复现…」）与 CONTEXT.md:230「SessionHost」词条（「消费 edge 传入的预授权投影」）经注 C' 裁决后描述被超越的形态，且差距较 iteration 1 扩大（推送 → 拉取、结局后 → 结局前过缝）。必须在 worker 形态票 SA8 前置门禁**之前或之中**正式落 ADR 修订/澄清附录（要素清单见 §6）；Host 可在本票实现同变更集自愿先行落 documentation-only 附录（**推荐非阻断**）。在此之前任何票不得援引机制句字面迫使回到「缓冲丢弃/edge 复现/结算回放」形态 | worker 形态票 + Host（先行可选） |
| R9' | **实现后 SA8 复查项（本版新增缝面落地核对）**：(i) `hub-split.ts`/`hub-edge.ts`/`hub-session.ts` 实际 diff 与本报告 §3 行 4/5 裁决逐项一致（`HubOpenAdmission` 三态无 revoked、promise 永不 reject 除台账缺失、beginAdmission 先于投递、shim 仅拉取）；(ii) edge 路由与 drain 判定读自身台账、不读 `sink.channels` 投影（代码级核对——worker 形态可迁移性的结构前提）；(iii) R4' 登记面维持（无可达测试面变化；若实现引入行为预检 port 成员消差 = 新缝面须再过 SA8）；(iv) R7 消除证实（M1-revoke 臂对 HEAD 基线逐点一致）；(v) R6' 泵预算证实（两锚 + M1 全绿）；(vi) M1/M2/M3/C0a~C0d/C1~C6 全绿 + AC5 三门禁 | 实现后 SA8 复查 |

## 9. Verdict

**`clear`**。§3 全表 22 项：16 项 no-conflict、6 项 implements-existing-decision；0 evolution-required、0 hard-conflict；无 override 被援引或需要。核心裁决（注 C'）：新缝面成员 `openAdmission` 经决策 2 三类约束逐层核验（生命周期四信号集不变 / 非被否决的逐帧接纳回执 / 结局纯 JSON）+ 决策 3 五点目的经代码亲读逐项保全（authorize 单点恰一次且 session 结构性不可达 / 被拒 ns 零会话资源唤起——`:355-359` 短路先于 `registry.open` / 失败面与闩锁由单份零 diff 通道原生产出 / 结算管线在 edge / 通道零 diff），裁为决策 3 authorize 成员的合法跨半边载体（**implements-existing-decision**）；「未授权 OPEN 不过缝」机制句的目的读法延伸**确认成立且较 iteration 1 更直接**——通道在场性与到达点效应（I13 两锚）本身即 AC4 冻结的既有测试面，字面读法与决策集自相矛盾。iteration 1 的 R4（pending 无界缓冲延期）**就地关闭**、R6/R8（推迟等价类 + 登记补全）**消解**、R7（revoked registry 短路微差）**消除**；新登记 R6'（微任务跳数）接受（纯泵预算面）。**设计可进入实现**，以 §8-R1/R2/R3（实现证据与必做测试）、R4'/R5/R7'（存续/关闭确认义务）、R9'（新缝面落地核对）为条件。

## 10. requiresConflictRecheck

**true**。依据：(1) **本报告裁决的形态本身待实现核对**——`openAdmission` 新缝面的落地产物（C0a/C0b stub 形态与 reject 臂、C0d 锁步与到达点断言、C2c/C2d 恰一次/入参一致、M1 到达点断言 + revoke 臂 HEAD 重定基、I13 两锚含 R6' 泵预算）必须逐项证实与裁决依据一致（§8-R1/R9'）；(2) 设计迁移连接状态机归属并重塑生命周期信号映射与 OPEN 准入管线（状态机/生命周期/失败语义三类均属「尚待实现核对」——iteration 1 §10 同款理由在本版形态上继续成立）；(3) §8-R4'（有界缓冲关闭确认 + worker 重入）、R5（worker 形态前置门禁、扩面）、R7'（ADR/CONTEXT 文本调和、差扩大）为跨票存续义务。实现 diff 落地后按本报告 §3 表 + §5 冻结面 + §8 行动逐项复核（SA8 implementation 复查触发条件成立：设计明示要求 + diff 触碰状态机/生命周期/失败语义面 + 新缝面）。
